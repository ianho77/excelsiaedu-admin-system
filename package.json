{
  "name": "excelsiaedu-admin-system-root",
  "version": "1.0.0",
  "description": "Root package.json for Vercel deployment",
  "scripts": {
    "build": "cd excelsiaedu-admin-system && npm install && npm run build",
    "vercel-build": "cd excelsiaedu-admin-system && npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
