MONGODB_URI=mongodb://127.0.0.1:27017/excelsiaedu-local
PORT=4000

# 只在需要從 MongoDB Atlas 下載資料到本地時填寫。
# 請不要把真正的 Atlas 連線字串提交到 GitHub。
ATLAS_MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
